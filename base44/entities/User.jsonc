{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "account_type": {
      "type": "string",
      "description": "Whether the user acts as a creator or an affiliate",
      "enum": [
        "creator",
        "affiliate"
      ],
      "default": "creator"
    },
    "first_name": {
      "type": "string",
      "description": "User first name"
    },
    "last_name": {
      "type": "string",
      "description": "User last name"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}