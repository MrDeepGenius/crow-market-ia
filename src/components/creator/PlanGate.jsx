import React from "react";

// PlanGate — Acceso libre al panel de creador.
// La creación de bots y productos es gratuita para todos los usuarios autenticados.
export default function PlanGate({ children, title }) {
  return children;
}